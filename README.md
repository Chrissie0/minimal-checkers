A two-player checkers game that runs in the terminal.

It saves previous match data (player names, date and outcome) into a text file called "oldgames.txt"

To run (you need jdk):
```
javac Checkers.java
java Checkers
```
